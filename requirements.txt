# Velox — Python dependencies
# Install with:  pip install -r requirements.txt

# Core ML
torch>=2.1.0
transformers>=4.40.0
accelerate>=0.27.0
bitsandbytes>=0.43.0        # 4-bit quantization (CUDA only; skip on pure CPU)
sentence-transformers>=2.7.0

# Search
elasticsearch>=8.12.0

# Utilities
numpy>=1.26.0
